version: '3.8'

services:
  mongo:
    image: mongo:6.0
    container_name: my-mongo
    restart: always
    ports:
      - 27017:27017
    volumes:
      - ./mongo-init.js:/docker-entrypoint-initdb.d/mongo-init.js:ro
      - mongo-data:/data/db

volumes:
  mongo-data:
